Today I try to charge phone using USB C port. But I notice it drops intermittently. The only way for it to start charging is you unplug and plug it again. Several searches suggest that we have USB suspend enabled.
Turns out this is caused by TLP. To disable this behavior, we go to `/etc/tlp.conf` and set `USB_EXCLUDE_PHONE=1`. This enables stable USB charging for phone only.