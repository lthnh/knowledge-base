[[Basic_MOSFET_Current_Mirror|Current mirrors]] can also be used to implement current steering circuits. Efforts is needed to ensure that each transistor is in saturation mode. P-channel transistors can also be used to implement current steering.

A transistor which pushes current into the circuit is called a *current source*. Conversely, a transistor which pulls current from the circuit is called a *current sink*.
